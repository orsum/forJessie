#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv) {
    int j, i = 0;
    if (argc >= 2){
        j = atoi(argv[1]);

        while (i < j){
            printf("%i\n", rand());
            i++;
        }
    
    }
        return EXIT_SUCCESS;
}
